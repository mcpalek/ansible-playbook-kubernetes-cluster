- hosts: workers
  #connection: local
  become: true
  #become_user: root
  roles:
  - base

- hosts: masters
  #connection: local
  become: true
  #become_user: root
  roles:
  - base

- hosts: workers
  #connection: local
  become: true
  # become_user: aca
  roles:
  - rocky #ubuntu #rocky

- hosts: masters
  #connection: local
  become: true
  # become_user: root
  roles:
  - rocky #ubuntu

- hosts: masters
  #connection: local
  become: true
  #become_user: root
  roles:
  - initkubernetes
#
- hosts: workers
  become: true
  #become_user: aca
  roles:
  - worker-join
